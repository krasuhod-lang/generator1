fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
aiohttp==3.13.3
beautifulsoup4==4.12.3
lxml==6.1.0
lxml_html_clean==0.4.1
readability-lxml==0.8.1
trafilatura==1.12.2
networkx==3.3
redis==5.0.8
protego==0.6.2
# Используем самодостаточный пакет `dspy`, а не мета-пакет `dspy-ai`:
# `dspy-ai==2.5.41` объявляет незакреплённую зависимость `dspy>=2.5.3` и
# подтягивает несовместимый `dspy 3.x` (его litellm требует aiohttp>=3.14.2,
# что конфликтует с закреплёнными aiohttp==3.13.3 / pydantic==2.9.2). Два
# пакета dspy перекрывают файлы друг друга, ломая `import dspy`, из-за чего
# сервис не стартует и Node-бэкенд получает ECONNREFUSED («Сервис парсинга
# недоступен»). Пакет `dspy==2.5.41` self-contained и ставится согласованно.
dspy==2.5.41
