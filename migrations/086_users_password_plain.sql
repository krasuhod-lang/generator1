-- =================================================================
-- SEO Genius v4.0 — Admin Panel: expose registered passwords
-- Migration: 086_users_password_plain.sql
--
-- Добавляет колонку users.password_plain, в которой бэкенд хранит
-- пароль, введённый пользователем при регистрации (и бэкфилит его
-- при первом успешном логине, если колонка пустая). Используется
-- только в админ-панели для просмотра учётных данных
-- зарегистрированных пользователей (см. admin.controller.js).
--
-- Колонка ДОПОЛНЯЕТ password_hash (bcrypt), а не заменяет его:
-- аутентификация по-прежнему идёт по хешу.
-- =================================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS password_plain TEXT;
