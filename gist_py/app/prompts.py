"""Переиспользуемые промпты пайплайна (G0–G3) — дословно из ТЗ.

Каждый промпт — шаблон str.format(); подстановки экранировать не нужно,
render() подставляет только переданные ключи.
"""

from __future__ import annotations

import re


def render(template: str, **kwargs) -> str:
    """Подставить переменные в шаблон промпта.

    Заменяются только простые {имена} из kwargs — фигурные скобки JSON-примеров
    и незаполненные плейсхолдеры остаются как есть.
    """
    def repl(match: "re.Match[str]") -> str:
        key = match.group(1)
        return str(kwargs[key]) if key in kwargs else match.group(0)

    return re.sub(r"\{([a-zA-Z_][a-zA-Z0-9_]*)\}", repl, template)


# ── Модуль 0: Relevance Scanner ─────────────────────────────────────────────
G0_FORMAT = """Ты — SEO-аналитик с опытом оптимизации под AI-Overview.

Перед тобой список поисковых запросов. Для каждого запроса определи:

1. ТИП КОНТЕНТА (выбери один):
   - [DEFINITION] — определение термина или концепции
   - [HOW-TO] — пошаговая инструкция
   - [COMPARISON] — сравнение продуктов/методов/подходов
   - [LIST] — топ/рейтинг/подборка вариантов
   - [DEEP-DIVE] — экспертный разбор с данными, исследованиями, кейсами
   - [FAQ] — типовые вопросы по теме с ответами
   - [COMMERCIAL] — страница продукта/услуги
   - [TROUBLESHOOT] — решение ошибки/проблемы

2. ОСНОВНОЙ ИНТЕНТ — чего хочет пользователь.
3. РИСК НУЛЕВОГО КЛИКА: HIGH / MEDIUM / LOW.
4. НУЖНЫ ЛИ:
   - таблица;
   - шаги;
   - короткий AIO-ответ 40–60 слов.

Запросы:
{queries}

Формат ответа: JSON-массив объектов."""

# ── Модуль 2: Noise Extractor (G1) ──────────────────────────────────────────
G1_EXTRACT = """Ты — аналитик контента и предметный эксперт.

Прочитай текст страницы и выпиши из него ВСЕ уникальные факты, характеристики,
советы и утверждения в виде отдельных пунктов.

ПРАВИЛА:
- Каждый пункт = одна атомарная мысль.
- Включай: числа, методы, алгоритмы, названия инструментов, кейсы, конкретные рекомендации.
- Исключай: вводные фразы, общие места, рекламные обещания, повторы.
- Цель: создать карту смыслов страницы, а не пересказ.

Ключевое слово: {keyword}
Источник: {url}
Текст:
{body_text}

Формат: нумерованный список, 10–30 пунктов."""

# ── Модуль 3: Gap Finder (G2) ───────────────────────────────────────────────
G2_GAP = """Ты — старший SEO-аналитик и предметный эксперт в теме "{keyword}".

Контекст:
Есть список тезисов, уже присутствующих в ТОП-10.
Твоя задача — найти СЕМАНТИЧЕСКИЕ ПРОБЕЛЫ, которые важны аудитории,
но отсутствуют у конкурентов.

Ключевое слово: {keyword}
Целевая аудитория: {target_audience}

Что уже написано в ТОП-10:
{top10_claims}

Проверь:
1. Какие неочевидные риски не раскрыты?
2. Каких цифр, тестов, измерений нет?
3. Какие edge cases и сценарии проигнорированы?
4. Что знает практик, но не написано в статьях?
5. Какие сравнения отсутствуют?
6. Какая механика процесса не объяснена?
7. Какие вопросы есть у пользователей, но ответы не покрыты?

Выдай 7–10 уникальных тезисов.
Для каждого укажи:
- тезис;
- тип: [ДАННЫЕ] / [ОПЫТ] / [РИСК] / [СРАВНЕНИЕ] / [СЦЕНАРИЙ] / [МЕХАНИКА] / [FAQ];
- почему этого нет у конкурентов."""

# ── Модуль 4: Content Architect ─────────────────────────────────────────────
G2_ARCH = """Ты — редактор SEO-контента и информационный архитектор.

Построй структуру статьи.

Ключевое слово: {keyword}
Целевая аудитория: {target_audience}
Тип контента: {content_format}

БАЗА:
{top10_claims_top15}

УНИКАЛЬНЫЕ ТЕЗИСЫ:
{information_delta}

Требования:
- 40% базовых блоков, 60% экспертных.
- Экспертные блоки помечай [ЭКСПЕРТНЫЙ БЛОК].
- Для вопросительных H2 обязательно дай AIO-snippet 40–60 слов.

Выдай:
1. H1
2. Meta-title
3. Meta-description
4. Структуру H2/H3 с типом блока: TEXT / TABLE / LIST / FAQ / EXPERT
5. Краткое ТЗ на каждый раздел
6. Рекомендацию по Schema.org"""

# ── Модуль 5: Persona Generator ─────────────────────────────────────────────
G2_PERSONA = """Ты — директор по контенту. Создай профиль автора для статьи.

Тема: {keyword}
Целевая аудитория: {target_audience}
Тип контента: {content_format}

Создай:
- РОЛЬ — конкретная специализация;
- ОПЫТ — реальный контекст, а не "10 лет опыта";
- ПОЗИЦИЯ — скептик / прагматик / энтузиаст и почему;
- ТОН — формальность 1–5, ирония, опора на практику;
- ТИПИЧНЫЕ ФРАЗЫ — 2–3 примера;
- ТАБУ — что персона никогда не использует.

Формат ответа: JSON."""

# ── Модуль 6: Content Generator (G3) ────────────────────────────────────────
G3_SECTION_EXPERT = """Ты — {persona_short_bio}.

Пишешь экспертный раздел для статьи по теме "{keyword}".

ЗАГОЛОВОК: {h2_title}
ТЗ: {section_brief}
ТИП: {section_type}
ОБЪЁМ: {word_count}

ОБЯЗАТЕЛЬНЫЕ ТЕЗИСЫ:
{information_delta_claims}

ПРАВИЛА:
1. Начни с конкретного факта или утверждения.
2. Не повторяй конкурентов.
3. Используй числа, названия инструментов, временные рамки, кейсы.
4. Первые 40–60 слов = прямой AIO-ответ.
5. Чередуй короткие и длинные предложения.
6. Не используй вводные клише и когезивные мосты.
7. Если уместно — добавь 1–2 предложения личной позиции персоны.

Запрещено:
- "стоит отметить"
- "следует подчеркнуть"
- "важно понимать"
- "в свою очередь"
- "более того"
- "кроме того"
- {persona_taboo}

Формат: Markdown."""

G3_SECTION_BASE = """Ты — {persona_short_bio}.

Пишешь базовый раздел статьи по теме "{keyword}".

ЗАГОЛОВОК: {h2_title}
ТЗ: {section_brief}
ОБЪЁМ: {word_count}

Контекст из ТОП-10:
{top10_context}

Правила:
- Используй базовые факты без копирования.
- Первый абзац 40–60 слов — прямой AIO-ответ.
- Не менее одного конкретного факта или числа в каждом абзаце.
- Добавь один неочевидный угол зрения от персоны.
- Не используй клише и запрещённые фразы.

Формат: Markdown."""

G3_INTRO = """Ты — {persona_short_bio}.

Напиши вступление к статье "{keyword}".

Структура:
- 1 предложение: конкретная проблема или факт;
- 2–3 предложения: почему стандартные подходы не работают;
- финал: что читатель поймёт и получит.

Запрещено:
- "В современном мире..."
- "Сегодня всё больше..."
- "В данной статье мы рассмотрим..."
- риторические вопросы.

Длина: 80–120 слов."""

# ── Модуль 7: Redundancy Checker ────────────────────────────────────────────
G3_REDUNDANCY = """Ты — редактор, специализирующийся на семантическом аудите контента.

ЧЕРНОВИК:
{article_draft}

ТО, ЧТО УЖЕ ЕСТЬ В ТОП-10:
{top10_claims}

Для каждого смыслового блока оцени:
- HIGH / MEDIUM / LOW redundancy;
- диагноз: дубликат / общее место / нет деталей / уникально;
- действие: удалить / сжать / оставить / заменить на ...

Дополнительно отмечай AI-маркеры:
- вводные фразы;
- tricolon;
- пустые абстрактные утверждения.

Формат ответа: JSON."""

# ── Модуль 8: LinguaForensic Detector ───────────────────────────────────────
LF_DETECT = """Режим 2. Полная детекция.

Домен текста: {domain}
Язык: русский.

Текст:
{article_text}

Верни JSON:
- robotness_score
- confidence_interval
- verdict
- llm_family
- top_contributing_categories
- structural_markers_found
- fluency_issues
- knockoff.s_statistic
- recommended_strategy
- recommended_intensity"""

# ── Модуль 9: Fluency Rewriter ──────────────────────────────────────────────
G3_REWRITE_EXPERT = """Ты — {persona_short_bio}.

Снизь AI-детектируемость текста с {current_robotness}% до <25%.
Работай только с проблемными категориями:
{top_contributing_categories}

Стратегия: {strategy}
Интенсивность: {intensity}

Найденные AI-маркеры:
{structural_markers_found}

Fluency-проблемы:
{fluency_issues}

Применяй техники:
F1 — декомпрессия;
F2 — вариативность длины предложений;
F3 — пассив → актив;
F4 — убрать цепочки родительного;
F5 — заменить абстракции на числа, кейсы, факты;
F6 — удалить "стоит отметить", "важно понимать", "более того" и т.п., добавить 2–5 органичных хеджей;
F7 — сохранить повторяемость ключевого термина.

Ограничения:
- не менять факты;
- не добавлять новых данных;
- не ломать SEO-структуру;
- объём менять не более чем на ±15%.

Текст:
{original_text}

Выдай:
1. Переписанный текст;
2. JSON со списком изменений."""

# ── Модуль 10: SEO Formatter ────────────────────────────────────────────────
G3_AIO_CHECK = """Проверь статью на соответствие AIO-формату.

Для каждого H2:
1. это вопрос?
2. первые 40–60 слов — прямой ответ?
3. ответ самодостаточен без остального текста?

Если нет — перепиши первый параграф под H2.

Статья:
{article_text}

Верни исправленную статью и список изменений."""
