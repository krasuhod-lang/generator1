fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
beautifulsoup4==4.12.3
lxml==6.1.0
lxml_html_clean==0.4.1
readability-lxml==0.8.1
trafilatura==1.12.2
pymorphy3==2.0.2
pymorphy3-dicts-ru==2.4.417150.4580142
rank-bm25==0.2.2
scikit-learn==1.5.2
numpy==1.26.4
# SEO Relevance Analyzer 2.0 (Phase 1) — факторная матрица + ранговые
# корреляции. scipy уже подтягивается scikit-learn'ом; фиксируем явно как
# ускоритель Spearman/Kendall (correlations.py работает и без него —
# чистый Python-fallback). pandas — для табличной сборки факторной матрицы
# и офлайн-пересчётов. Обе — «мягкие» зависимости: код не падает при их
# отсутствии, но с ними быстрее и точнее.
scipy==1.11.4
pandas==2.2.3
