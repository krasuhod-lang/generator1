'use strict';

/**
 * targetPageAnalyzer.js — анализатор целевой страницы и домена.
 *
 * Парсит контент целевой страницы (input_target_url) и отправляет
 * его на AI-анализ для определения:
 *   - целевая аудитория
 *   - особенности ниши
 *   - ограничения проекта
 *   - факты о бренде / продукте / услуге
 *
 * Результаты используются для автозаполнения пустых полей задачи
 * и для обогащения контекста генерации контента.
 */

const { scrapeUrl }    = require('./scraper');
const { callLLM }      = require('../llm/callLLM');

const TARGET_PAGE_ANALYSIS_PROMPT = `Ты — Senior аналитик контента, эксперт по E-E-A-T (Опыт, Экспертность, Авторитетность, Достоверность) и бизнес-разведке. Твоя задача — не сделать «дамп страницы», а провести АНАЛИТИЧЕСКИЙ разбор: за каждым фактом увидеть скрытый смысл, доказательство компетентности или, наоборот, провал доверия.

═══════════════════════════════════════════
ВХОДНЫЕ ДАННЫЕ
═══════════════════════════════════════════
URL целевой страницы: {{TARGET_URL}}
Контент страницы:
{{PAGE_CONTENT}}

═══════════════════════════════════════════
ЗАДАЧА (АНАЛИТИКА, А НЕ ПАРСИНГ)
═══════════════════════════════════════════
Категорически запрещено просто пересказывать содержимое страницы. Каждый раздел отчёта = аналитический вывод с раскрытием смыслов. Чистое перечисление без интерпретации — провал задачи.

1. TARGET_AUDIENCE — целевая аудитория с эмоциональным состоянием:
   - Кто эти люди (демография: возраст, пол, доход, статус, география).
   - В КАКОМ СОСТОЯНИИ они приходят на эту страницу ПРЯМО СЕЙЧАС: напуганы диагнозом / ищут второе экспертное мнение / хотят сэкономить / устали от плохого опыта с конкурентами / в режиме «горящей» задачи / сравнивают и проверяют / уже выбрали и ищут подтверждение?
   - Какую конкретную проблему/боль они пытаются закрыть, попадая на эту страницу?
   - Какие сомнения/возражения у них в голове в момент чтения?
   - Описание РАЗВЁРНУТОЕ (4–6 предложений), с эмоциональным окрашиванием, не сухой портрет.

2. NICHE_FEATURES — особенности ниши:
   - Тип ниши (YMYL / не-YMYL / трастозависимая / эмоциональная / экспертная).
   - Сезонность, локальная привязка, юридические/регуляторные требования.
   - Специфика принятия решения (импульс / длинный цикл / коллегиальное / страх ошибки).
   - Уровень конкуренции и кто доминирует (агрегаторы / бренды / эксперты-инфлюенсеры).
   - Каждая особенность — РАЗВЁРНУТОЕ описание (1–2 предложения с практическим следствием).

3. PROJECT_LIMITS — ограничения проекта (АУДИТ ОТСУТСТВУЮЩЕГО E-E-A-T):
   ⚠️ Это самое важное поле для безопасности будущих статей. Здесь фиксируется ВСЁ, чего на сайте НЕТ.
   Минимум 6–10 пунктов, каждый — конкретное отсутствие, а не общие слова. Примеры формулировок:
   - «Нет раздела с врачами/экспертами и их регалиями — нельзя ссылаться на конкретных авторов».
   - «Цены не указаны (только "по запросу") — в статье запрещены конкретные суммы и обещания "от N руб."».
   - «Нет отзывов с независимых площадок (Яндекс/2GIS/Google) — нельзя писать "сотни довольных клиентов"».
   - «Нет указания опыта в годах и цифр (количество кейсов / процедур / клиентов) — запрещены утверждения "ведущая клиника" / "лидер рынка"».
   - «Нет лицензий/сертификатов на видном месте — нельзя обещать соответствие стандартам».
   - «Нет описания процесса/протоколов работы — нельзя обещать "индивидуальный подход" без подтверждения».
   - «Нет фото оборудования / клиники / производства — нельзя апеллировать к технологичности».
   - «Нет публикаций в СМИ / научных публикаций — запрещены формулировки "признанный эксперт"».
   Цель: дать копирайтеру чёткий список «что писать НЕЛЬЗЯ, потому что бренд этого не доказал».

4. BRAND_FACTS — АНАЛИТИЧЕСКИЙ дайджест бренда (НЕ ДАМП кода/контента):
   ⚠️ Запрещено: просто перечислять цены, услуги, телефоны построчно. Это работа парсера, а не аналитика.
   Требуется: связный текст, в котором каждый факт показан в связке «что бренд делает → КАК он доказывает свою компетентность → КАКУЮ боль клиента это закрывает».
   Структурируй внутренними подзаголовками (простым текстом, без markdown):
   - «Что предлагает»: продукт/услуга и её ключевая ценность для клиента (не прайс, а смысл).
   - «Как доказывает компетентность»: упоминания врачей/экспертов с регалиями, ISO/сертификаты, стандарты, опыт в цифрах («более 27 лет», «5 000+ операций», «команда из 12 PhD»), оборудование, технологии, упомянутые методики/протоколы.
   - «Trust-сигналы и социальные доказательства»: отзывы (где, сколько), кейсы (с конкретикой), публикации в СМИ, награды, партнёрства, клиентская база.
   - «Цифры и обещания»: только те, что РЕАЛЬНО есть на странице, со ссылкой на формулировку.
   - «Уникальные смыслы (USP) и тон»: чем бренд позиционно отличается, какую эмоцию транслирует (экспертность / забота / технологичность / премиум / доступность).
   - «Контекст принятия решения»: как страница ведёт клиента, какие триггеры и CTA использует.
   Если поле пустое (на странице нет фактов) — честно напиши «бренд не предоставил доказательств в категории X».

5. EEAT_SIGNALS_PRESENT — массив строк: какие конкретные E-E-A-T сигналы РЕАЛЬНО есть на странице (например: «Указан главный врач с фото и регалиями (д.м.н., 30 лет опыта)», «Лицензия Минздрава ЛО-77-...», «Видеоотзывы пациентов с лицами», «Описание витрификации по протоколу Cryotop»).

6. EEAT_SIGNALS_MISSING — массив строк: какие критичные E-E-A-T сигналы ОТСУТСТВУЮТ и должны быть учтены копирайтером как «нельзя обещать». Дублирует логику project_limits, но в формате коротких маркеров для machine-friendly использования.

7. SERVICE_DETAILS — аналитическое описание услуг/продуктов:
   - Что входит, как работает, какой результат гарантируется (только заявленный).
   - Этапы процесса с пользой клиенту на каждом шаге.
   - Условия, сроки, форматы взаимодействия.
   - Уникальные преимущества (USP) с расшифровкой «почему это важно».

8. PROOF_ASSETS — анализ доказательной базы:
   - Какие доказательства есть и насколько они сильные (фото с лицами / анонимные / с фамилиями).
   - Где размещены отзывы (свой сайт / независимые платформы).
   - Конкретика: количество, рейтинги, ссылки на источники.
   - Чего не хватает в доказательной базе.

═══════════════════════════════════════════
ФОРМАТ ОТВЕТА
═══════════════════════════════════════════
Верни строго JSON без markdown-обёрток:
{
  "target_audience": "развёрнутое описание ЦА с эмоциональным состоянием на момент визита (4-6 предложений)",
  "niche_features": ["особенность 1 с развёрнутым описанием", "..."],
  "project_limits": ["ограничение 1 (отсутствующий E-E-A-T элемент)", "ограничение 2", "..."],
  "brand_name": "название бренда или null",
  "brand_facts": "АНАЛИТИЧЕСКИЙ связный текст со внутренними подзаголовками («Что предлагает:», «Как доказывает компетентность:», «Trust-сигналы:», «Цифры и обещания:», «USP и тон:», «Контекст принятия решения:»). НЕ дамп страницы, а раскрытие смыслов.",
  "eeat_signals_present": ["конкретный сигнал 1", "сигнал 2", "..."],
  "eeat_signals_missing": ["отсутствующий сигнал 1", "..."],
  "service_details": "аналитическое описание услуг с пользой и USP",
  "proof_assets": "анализ доказательной базы (что есть, насколько сильно, чего не хватает)",
  "detected_region": "регион работы компании или null",
  "detected_business_type": "тип бизнеса (SaaS/e-commerce/услуги/etc) или null",
  "detected_business_goal": "предполагаемая бизнес-цель (лиды/продажи/трафик/etc) или null",
  "detected_site_type": "тип сайта (новый/растущий/зрелый/etc) или null"
}

═══════════════════════════════════════════
ПРАВИЛА (СТРОГО)
═══════════════════════════════════════════
- Извлекай ТОЛЬКО то, что РЕАЛЬНО присутствует в контенте страницы. Не выдумывай цифры, имена, лицензии, сертификаты.
- Если информации нет — для строковых полей пиши null или «бренд не предоставил данных», для массивов — [].
- brand_facts ОБЯЗАТЕЛЬНО аналитический (со связками «факт → доказательство → польза»). Простое перечисление цен и услуг = ошибка.
- project_limits и eeat_signals_missing — обязательно содержательные (не «всё хорошо»). Хорошая аналитика всегда находит пробелы.
- target_audience обязательно с эмоциональным состоянием на момент визита, не сухая демография.
- СТРОГО JSON, никакого markdown, никакого текста вне JSON.

[SELF-AUDIT — silent, do not output]
  ✓ В brand_facts видна связка «факт → как доказывает компетентность → польза клиенту», а не дамп?
  ✓ В project_limits перечислены КОНКРЕТНЫЕ отсутствующие E-E-A-T-элементы (минимум 6)?
  ✓ В target_audience зафиксировано эмоциональное состояние, а не только демография?
  ✓ Все цифры/имена/лицензии в отчёте РЕАЛЬНО присутствуют в контенте страницы?
  ✓ eeat_signals_present и eeat_signals_missing согласованы (нет противоречий)?`;

/**
 * analyzeTargetPage — анализирует целевую страницу и домен.
 *
 * @param {string}  targetUrl — URL целевой страницы
 * @param {object}  ctx       — { log, taskId, onTokens }
 * @returns {object|null}     — результат анализа или null при ошибке
 */
async function analyzeTargetPage(targetUrl, ctx) {
  const { log, taskId, onTokens } = ctx;

  if (!targetUrl || !targetUrl.trim()) {
    log('Target Page Analyzer: URL не указан — пропускаем', 'info');
    return null;
  }

  const url = targetUrl.trim();

  // Validate URL format
  try {
    new URL(url);
  } catch {
    log(`Target Page Analyzer: некорректный URL "${url}" — пропускаем`, 'warn');
    return null;
  }

  log(`Target Page Analyzer: парсинг целевой страницы ${url}...`, 'info');

  // Scrape the target page
  let pageData;
  try {
    pageData = await scrapeUrl(url, 25000);
  } catch (err) {
    log(`Target Page Analyzer: ошибка парсинга ${url}: ${err.message}`, 'warn');
    return null;
  }

  if (!pageData || !pageData.markdown || pageData.markdown.length < 100) {
    log(`Target Page Analyzer: недостаточно контента на ${url} (${(pageData?.markdown || '').length} символов)`, 'warn');
    return null;
  }

  log(`Target Page Analyzer: получен контент (${pageData.markdown.length} символов). Запуск AI-анализа...`, 'info');

  // Build the analysis prompt
  const pageContent = pageData.markdown.substring(0, 15000);
  const prompt = TARGET_PAGE_ANALYSIS_PROMPT
    .replace('{{TARGET_URL}}', url)
    .replace('{{PAGE_CONTENT}}', pageContent);

  try {
    const analysisResult = await callLLM(
      'deepseek',
      'Ты — Senior аналитик контента и эксперт по E-E-A-T. Возвращай структурированный JSON. СТРОГО из контента, без галлюцинаций. brand_facts — аналитический связный текст со связками «факт → как доказывает компетентность → польза клиенту», НЕ дамп страницы. project_limits и eeat_signals_missing обязательно фиксируют ОТСУТСТВУЮЩИЕ E-E-A-T-элементы. target_audience обязательно с эмоциональным состоянием на момент визита.',
      prompt,
      {
        retries:     2,
        taskId,
        stageName:   'target_page_analysis',
        callLabel:   'Target Page Analysis',
        temperature: 0.2,
        log,
        onTokens,
      }
    );

    if (!analysisResult || typeof analysisResult !== 'object') {
      log('Target Page Analyzer: AI вернул некорректный результат', 'warn');
      return null;
    }

    log(
      `Target Page Analyzer: анализ завершён. ` +
      `Бренд: ${analysisResult.brand_name || 'не определён'}, ` +
      `Регион: ${analysisResult.detected_region || 'не определён'}, ` +
      `Тип бизнеса: ${analysisResult.detected_business_type || 'не определён'}`,
      'success'
    );

    return analysisResult;
  } catch (err) {
    log(`Target Page Analyzer: AI-анализ ошибка: ${err.message}`, 'warn');
    return null;
  }
}

module.exports = { analyzeTargetPage };
